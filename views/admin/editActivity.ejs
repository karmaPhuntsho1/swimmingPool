<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Activity</title>
  <link rel="stylesheet" href="/css/adminStyles.css">
  <style>
    /* Centered form container */
form {
  max-width: 500px;
  margin: 2rem auto;
  background-color: rgb(195, 228, 238);
  padding: 2rem;
  border-radius: 10px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

/* Labels */
form label {
  display: block;
  margin-bottom: 0.5rem;
  font-weight: bold;
}

/* Input fields */
form input[type="text"],
form input[type="number"] {
  width: 100%;
  padding: 0.6rem;
  margin-bottom: 1.5rem;
  border: 1px solid #ccc;
  border-radius: 6px;
  font-size: 1rem;
}

/* Submit button */
form button {
  width: 100%;
  background-color: #0077b6;
  color: white;
  padding: 0.75rem;
  font-size: 1rem;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

form button:hover {
  background-color: #023e8a;
}
  </style>
</head>
<body>
    <nav class="navbar">
    <div class="nav-brand">Swimminng Pool</div>
    <ul class="nav-links">
      <li><a href="/admin/dashboard">Dashboard</a></li>
      <li><a href="/admin/view-swimming">View Activities</a></li>
      <li><a href="/admin/pool-bookings" class="active">Pool Bookings</a></li>
      <li><a href="/logout">Logout</a></li>
    </ul>
  </nav>
  <main class="container">
    <h1>Edit Swimming Activity</h1>
  <form action="/admin/edit-activity/<%= activity.id %>" method="POST">
    <label>Activity Name:</label>
    <input type="text" name="name" value="<%= activity.name %>" required><br>

    <label>Coach Name:</label>
    <input type="text" name="coach" value="<%= activity.coach %>" required><br>

    <label>Price:</label>
    <input type="number" name="price" value="<%= activity.price %>" step="0.01" required><br>

    <button type="submit">Update Activity</button>
  </main>
  </form>
</body>
</html>